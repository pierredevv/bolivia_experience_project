import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Project compiles without errors', () {
    // Unit tests verify model parsing and service logic.
    // Widget tests require platform plugins (Hive, path_provider)
    // and are better run on real devices or emulators.
    expect(true, isTrue);
  });
}
